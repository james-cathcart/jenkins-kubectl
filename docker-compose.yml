---
version: "3.8"

services:

  jenkins-blue:
    container_name: jenkins
    build:
      context: ./context
    image: 178.168.1.11:32000/jenkins-kubectl:0.0.1
    ports:
      - "80:8080"
    volumes:
      - type: bind
        source: ~/volumes/jenkins-data
        target: /var/jenkins
